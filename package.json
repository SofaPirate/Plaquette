{
  "name": "Plaquette",
  "version": "0.9.3",
  "devDependencies": {
    "auto-changelog": "*"
  },
  "scripts": {
    "version": "bash bump_version.sh $npm_package_version && auto-changelog -p && git add CHANGELOG.md library.properties docs/conf.py"
  }
}
